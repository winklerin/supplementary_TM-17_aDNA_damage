libmerge (https://vcs.eva.mpg.de/git_archgen/libmerge) is used to retrieve fastq files, and merge different sequencing libraries at the individuals level.

To execute libmerge, first install it (see link above), and then run the Makefile present in this directory.